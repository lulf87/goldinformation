# 运行时覆盖配置（用于覆盖 .env 中的同名键）
#
# 切回 OpenRouter + 便宜模型
LLM_ENABLED=true
LLM_PROVIDER=openrouter
LLM_MODEL=openai/gpt-4o-mini

# 如果你已经在 .env 里配置了 OPENROUTER_API_KEY，这里可以不写。
# 否则请在这里填写：
# OPENROUTER_API_KEY=sk-...
